# Load Balanced Network Example Instructions
- Ensure multipass is installed on your system. For Instructions see https://canonical.com/multipass/install 
- Once multipass is installed, run `bash run_network.sh` to create the virtual machine. 